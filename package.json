{
  "name": "gameweek",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm --recursive --filter './apps/*' run build && node scripts/build/assemble-site.mjs",
    "test": "node --test scripts/kickoff-backfill/*.test.mjs && scripts/build/parity-test.sh"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
